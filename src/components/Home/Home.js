import React, { Component } from 'react';

export default class Home extends Component{
   render(){
      return(
         <React.Fragment>
            <p>HomePage works!!</p>
         </React.Fragment>
      );
   }
}
